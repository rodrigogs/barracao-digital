const { name: ROOT_PACKAGE_NAME } = require('../package.json');

const STAGE = process.env.NODE_ENV;

const {
  AWS_REGION,
  AWS_ACCOUNT_ID,
  STACK_NAME = ROOT_PACKAGE_NAME,
  GOOGLE_DISTANCES_MATRIX_API_KEY = '',
  GOOGLE_APPLICATION_CREDENTIALS = '',
  OPENTOK_API_KEY = '',
  OPENTOK_API_SECRET = '',
  CONFIGS_TABLE = `${ROOT_PACKAGE_NAME}-${STAGE}-configs`,
  DOCTORS_TABLE = `${ROOT_PACKAGE_NAME}-${STAGE}-doctors`,
  PATIENTS_TABLE = `${ROOT_PACKAGE_NAME}-${STAGE}-patients`,
  FACILITIES_TABLE = `${ROOT_PACKAGE_NAME}-${STAGE}-facilities`,
  DOCTOR_INACTIVITY_TIMEOUT_IN_MINUTES = 33,
  CONVERSATION_CLEANUP_TIMEOUT_IN_MINUTES = 60,
} = process.env;

module.exports = {
  AWS_REGION,
  AWS_ACCOUNT_ID,
  STAGE,
  APP_NAME: ROOT_PACKAGE_NAME,
  STACK_NAME,
  ROOT_PACKAGE_NAME,
  GOOGLE_DISTANCES_MATRIX_API_KEY,
  GOOGLE_APPLICATION_CREDENTIALS,
  OPENTOK_API_KEY,
  OPENTOK_API_SECRET,
  CONFIGS_TABLE,
  DOCTORS_TABLE,
  PATIENTS_TABLE,
  FACILITIES_TABLE,
  DOCTOR_INACTIVITY_TIMEOUT_IN_MINUTES,
  CONVERSATION_CLEANUP_TIMEOUT_IN_MINUTES,
};
