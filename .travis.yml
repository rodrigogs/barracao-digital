language: node_js

node_js:
  - "12"

cache: npm

install:
  - npm install

script:
  - npm run deploy

after_script:
  - npm run clear-frontend-cache

branches:
  only:
  - master
  - development

notifications:
  email:
    on_success: never
    on_failure: always
